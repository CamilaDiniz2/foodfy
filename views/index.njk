{% extends "layout.njk" %}

  {% block head %}
      <title> .: Pagina Inicial - Foodfy </title>
  {% endblock %}

{% block content %}

<section class="principal-image">
  <div class="text-image">
    <h1>As melhores receitas</h1>

    <p>Aprenda a construir os melhores pratos com receitas
      criadas por profissionais do mundo inteiro.
    </p>
  </div>

  <div class="logo-image">
    <img src="images/chef.png" alt="">
  </div>
</section>

<div class="mais-acessadas">
  <h2>Mais acessadas</h2>

  <div class="pedidos-acessados">

    {% for item in items %}
    <div class="pedido">
      <img src="{{item.image}}" alt="Foto de um hamburguer">
      <p class="title">{{item.title}}</p>
      <p class="subtitle">{{item.author}}</p>
    </div>
    {% endfor %}

  </div>
</div>


{% endblock %}
     